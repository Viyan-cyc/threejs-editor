# 模型资产目录

按领域分子目录放置 GLB/GLTF 文件，并在对应 `src/domains/<domain>/assets.ts` 中登记 `AssetEntry`。

- `energy/` — solar-panel.glb, power-station.glb ...
- `industrial/` — factory.glb, warehouse.glb ...
- `ict/` — base-station.glb, signal-tower.glb ...
- `smart-home/` — room.glb, device.glb ...

未登记或缺失的资产，ModelFactory 会返回占位盒体（便于早期联调）。
